monitor.alsa.rules = [
  {
    matches = [
      {
        # Matches all sources
        node.name = "bakruis"
      },
      {
        # Matches all sinks
        node.name = "bakruis"
      }
    ]
    actions = {
      update-props = {
        session.suspend-timeout-seconds = 0
      }
    }
  }
]
